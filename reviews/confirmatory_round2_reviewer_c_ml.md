# Confirmatory round 2 — machine learning and reproducibility reviewer

## Critical issues

None remaining.

## Important issues resolved

- The primary baseline differs only by the 15 response columns.
- Standardized-equivalence exclusions, refit split invariants, shuffled controls,
  global exclusions and zero-ARROW transfer are machine-readable.
- Evaluation predictions are separated from the all-data deployment refit.
- Tests derive smoke values from the artifact manifest and verify the feature schema.

## Optional submission-stage polish

- Archive a DOI-tagged release when the journal submission is created.
